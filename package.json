{
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"npm run start:eleventy --watch\" \"npm run start:esdev\"",
    "start:eleventy": "eleventy --watch",
    "start:esdev": "es-dev-server --node-resolve --open --watch --root-dir ./_site",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "0.10.0",
    "es-dev-server": "^1.46.4",
    "concurrently": "^5.1.0"
  }
}